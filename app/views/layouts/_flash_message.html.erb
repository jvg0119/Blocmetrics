<%# flash[:success] = 'test flash' %>

<% flash.each do |key, msg| %>
	<div class="alert alert-<%= flash_type(key) %>">
		<button type="button" class="close" data-dismiss="alert">×</button>
		<%= msg %>
	</div>
<% end %>
