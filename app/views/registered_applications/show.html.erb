
<h1><%= @registered_application.name %></h1>
<h4><%= @registered_application.url %><h4>

<%= link_to "Edit", edit_registered_application_path(@registered_application), class: "btn btn-md btn-primary" %>
<%= link_to "Delete", registered_application_path(@registered_application), method: :delete, data: { confirm: "Are you sure?"}, class: "btn btn-md btn-danger"  %>

<%= link_to "All registered applications", registered_applications_path, class: "btn btn-md btn-success" %>

<!-- below is replace w/ the chart -->
<% if @events.any? %>
	<h4><%= pluralize @events.count, "Event" %></h4>
	<%#= @events.inspect %>
	<%#= @events %>
	<%#= render partial: 'event', collection: @events %>
	<% @events.each do |key, value| %>
		<%#= key %>
		<%#= value %>
		<%#= pie_chart @registered_application.events.group(:name).count %>
	<% end %>
<% end %>


<%= pie_chart @registered_application.events.group(:name).count %>
<%#= column_chart Order.group_by_day_of_week(:created_at, format: "%a").count %>
<%#= column_chart @registered_application.events.group_by(:name).count %>

<%= line_chart @registered_application.events.group_by_hour(:created_at).count %>
<%#= line_chart @registered_application.events.group_by_day(:created_at).count %>

<%#= column_chart @registered_application.events.group_by_minute(:created_at).count %>
